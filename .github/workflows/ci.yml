name: CI

on:
  push:
    branches: [master, main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-test:
    name: Build + Lint + Unit Test + Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: ${{ runner.os }}-gradle-
      - name: Grant execute permission
        run: chmod +x gradlew
      # 两条工程红线此前只靠人工自觉执行，CI 从未调用 → 实际无门禁。
      # 二者均为零依赖静态检查（runner 自带 python3 + node20），秒级完成，放在编译前快速失败。
      - name: Gate - Room 迁移与 schema 一致性
        run: python3 test/check_migrations.py
      - name: Gate - strings.xml 引用完整性
        run: node test/scan_strings.mjs
      - name: Assemble debug
        run: ./gradlew :app:assembleDebug --no-daemon
      # release 变体启用 R8 + shrinkResources。此前 CI 从不构建 release，
      # proguard keep 规则缺漏（Opus native / SQLCipher JNI 等）只能在发版后暴露。
      # 无 release 签名时 build.gradle.kts 会自动回退 debug 签名，CI 上可直接构建。
      - name: Assemble release (R8 + shrinkResources gate)
        run: ./gradlew :app:assembleRelease --no-daemon
      - name: Lint (release, abortOnError)
        run: ./gradlew :app:lintRelease --no-daemon
      - name: Unit tests + Jacoco coverage
        run: ./gradlew :app:createDebugUnitTestCoverageReport --no-daemon
      - name: Upload unit-test coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-coverage
          path: app/build/reports/coverage/debug

  instrumented-test:
    name: Instrumented Test (Emulator)
    runs-on: ubuntu-latest
    # 模拟器 job 约 15 分钟。编译/单测秒级挂掉时没必要白跑，串行以节省 runner 配额。
    needs: unit-test
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: ${{ runner.os }}-gradle-
      - name: Grant execute permission
        run: chmod +x gradlew
      - name: Run instrumented tests on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          target: google_apis
          arch: x86_64
          profile: pixel_6
          script: ./gradlew :app:connectedDebugAndroidTest --no-daemon
      - name: Upload instrumented test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: instrumented-test-results
          path: app/build/reports/androidTests/connected
