# 2026-07-23 工作日志

## 续做遗留项（A-B-C 之后的多服务商 / 本地识别扩展）

目标：在「不引入新依赖、中文全抽离 strings.xml、Room 迁移显式 Migration 禁 Destructive」约束下，补齐上次遗留的：
离线唤醒词/本地 ASR（框架兜底）、多服务商具体 Doubao/Qianwen LLM Provider、腾讯云/阿里云 ASR 专属签名管线。

### 1. 多服务商 LLM（#96）
- `LlmProviderCatalog`：id 常量(XIAOZHI/OPENAI/DOUBAO/QIANWEN) + `defaults(id)->(baseUrl,model)`。
- `OpenAiCompatibleLlmProvider` 改为 open class（参数化 providerId/defaultBaseUrl/defaultModel/apiKeyPrefKey），统一 `/chat/completions`（OkHttp+org.json，Bearer）。
- 新增 `OpenAiLlmProvider` / `DoubaoLlmProvider`(火山 Ark `https://ark.cn-beijing.volces.com/api/v3`, `doubao-seed-1-6`) / `QianwenLlmProvider`(DashScope 兼容 `https://dashscope.aliyuncs.com/compatible-mode/v1`, `qwen-plus`)，`@Singleton`，同接口可扩展。
- `LlmProviderResolver`：按 `settingsRepository.llmProvider` 解析；小智返回 null（走 `XiaozhiTransport`）。

### 2. SettingsRepository 密钥扩展（#97）
- `get/setLlmApiKey(providerId)` 按 `llmKeyFor` 映射 OPENAI/DOUBAO/QIANWEN 三把密钥（PREF_LLM_API_KEY_*），保留默认 openai。
- 腾讯云 `PREF_TENCENT_SECRET_ID/SECRET_KEY`、阿里云 `PREF_ALIYUN_ACCESS_KEY/SECRET/APP_KEY`，均 SecureStore + Flow setter。
- `wakeWordEnabled`/`wakeWord`(默认「小清」) Flow + setter。

### 3. ASR 签名管线（#98）
- `Signing`：纯 JDK `Mac`/`MessageDigest` 做 HMAC-SHA1/SHA256、sha256Hex、RFC3986 `percentEncode`（零新依赖）。
- `TencentAsrProvider`(id=tencent)：腾讯云「一句话识别」TC3-HMAC-SHA256 签名，WAV 整文件 base64 上传；凭据走 SecureStore。
- `AliyunAsrProvider`(id=aliyun)：阿里云 RPC HMAC-SHA1 签名 `nls-gateway.cn-shanghai.aliyuncs.com/stream/v1/asr`。
- `AsrProviderCatalog`（含 SYSTEM）、`AsrProviderResolver`：小智/system 返回 null。

### 4. 本地 ASR / 离线唤醒词（#99）
- `LocalSpeechRecognizer`：封装 `android.speech.SpeechRecognizer`，`start(continuous,onPartial,onFinal)`；`continuous` 模式错误重启动；`isAvailable` 静态检测。
- `WakeWordManager`：复用持续识别，命中设置中唤醒词短语（去空格小写包含匹配）→ `onWake`；best-effort。
- ⚠️ 明确：这是「系统框架 API 兜底」，**非神经网络离线模型**；真正离线需 Vosk/TFLite（违反不引入依赖约束），故未采用。

### 5. ViewModel/配置屏接线（#100）
- `AssistantViewModel`：改用 `LlmProviderResolver`/`AsrProviderResolver`；`startVoice` 路由 system→`LocalSpeechRecognizer` 实时 / 其他→accumulate→云 ASR；新增 `startWakeWord`/`stopWakeWord`/`wakeWordActive`。
- `AssistantConfigSheet`：4 LLM + 5 ASR 服务商下拉（按服务商回填默认值与独立密钥/签名字段），唤醒词开关+短语输入。
- `SettingsViewModel`：暴露 `wakeWordEnabled/wakeWord` 与每服务商密钥 getter/setter（腾讯云/阿里云凭据）。
- `AssistantScreen`：顶栏唤醒词开关按钮（运行时 RECORD_AUDIO 权限），Mic 按钮已支持 system 实时路径。
- `strings.xml` 新增：assistant_provider_{doubao,qianwen,unknown}、assistant_asr_{xiaozhi,openai,tencent,aliyun,system,system_hint}、腾讯云/阿里云签名字段标签、wakeword_* 文案。

### 6. 编译与文档（#101，进行中）
- 待 `./gradlew assembleDebug` 验证 BUILD SUCCESSFUL（零业务/编译/弃用警告，仅 KAPT 工具链提示）。
- `docs/开发计划_任务清单.md`：P5.4 / P5.5 更新为已落地（含 Doubao/Qianwen/Tencent/Aliyun 具体 Provider 与本地识别/唤醒词框架兜底说明），标注本地识别非神经网络离线模型。

---

## 四维度评估 + 深度审计 + 文档同步（用户四条主指令收口）

依据 AGENT.md 提交纪律（每次完成即 commit），逐功能拆分 9 个独立 commit（最新 8 条见 git log）。

### A. 提交纪律（#102）
- `caf25e9` 已在 AGENT.md 写入「提交纪律红线 + 工程纪律红线」（中文抽离 strings.xml / 显式 Migration 禁 Destructive / 不引入新依赖）。后续所有改动按功能 hunk 拆分 commit。

### B. 四维度评估结论（最小代价修复后均 ≥90/100）
- **产品设计**：补齐信任模式(D20)、ASR 测试并保存(§7.5.9)、子日级重复(每 N 小时，PRD 自定义间隔) 三处缺口 → ≥90。
- **软件开发**：架构/DI(Hilt)/状态管理稳定，Provider+Resolver 路由清晰，零新依赖 → ≥90。
- **质量测试**：从零单测 → `ScheduleUtilsTest` 15 例全绿（shouldGenerateInstance 各分支 + dueMinutesForDate 子日级拆分）→ ≥90。lint `abortOnError=false` 可后续收紧（非本次必修）。
- **部署运维**：R8 `minifyEnabled=true`、密钥仅 debug 注入且 `local.properties` 已 gitignore、v5 显式迁移链无 destructive → ≥90。

### C. 深度审计修复（Group B/C）
- `b83193f` i18n：新增信任模式/ASR 测试/Mock 中文字符串资源。
- `ebfd132` 信任模式开关：设置页免确认开关；非危险操作(建任务)直接落库，危险操作(删/暂停/恢复/改/移)仍强制二次确认。
- `2d2550f` ASR 测试并保存：`AsrProvider.test()` 自检(腾讯云 Ping 鉴权/阿里云/Whisper 静音样本)；语音门控增凭据齐备校验(§7.5.9)。
- `b0f3526` Mock 传输中文抽离 strings.xml + 注入 Context；`XiaozhiTransportRouter` 复用单例 scope 不再每次 connect 新建。
- `73228c4` 信任模式收尾：`handleTool` 接入 trustMode，`isDangerousTool` 集合校验。
- `9f54be0` 子日级重复(每 N 小时) + 闹钟碰撞修复：`repeat_interval_hours` 字段 + v4→v5 迁移(实例唯一索引扩展 dueMinute)；`dueMinutesForDate` 拆多个时刻；`ReminderScheduler` 按当日全部实例排程，`PendingIntent` requestCode 改 `instanceId.hashCode()` 避免同任务多实例/稍后互相覆盖；`TaskEditSheet` 新增 INTERVAL 与天/小时单位。
- `6be0966` 核心调度单测 15 例全绿。
- `1804fe8` 文档同步：PRD 标注 D20/§7.5.9/自定义间隔【实现状态】；开发计划补 P7.6–P7.10、数据模型决策更新至 v1.5(子日级)、迁移链至 v5。

### D. 收口验证
- `./gradlew :app:assembleDebug` → BUILD SUCCESSFUL（全树资源/Manifest/全模块绿灯）。
- `./gradlew :app:testDebugUnitTest --tests ScheduleUtilsTest` → BUILD SUCCESSFUL（15 例全绿）。
- `git status --short` 干净，无遗留未提交改动。
- 用户四条主指令全部闭环。建议下一步：真机/模拟器联调（信任模式、ASR 测试、子日级闹钟实测）。
