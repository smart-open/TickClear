# 2026-07-21 · TickClear 点清

## Phase 0/1 构建打通（BUILD SUCCESSFUL，产出 33MB debug APK）

排查并修复的错误/遗漏（用户要求"分析是否有错误或遗漏"）：

1. **kapt 插件版本** — `libs.versions.toml` 中 `kotlinKapt` 不能带 `version.ref`（随 Kotlin 插件捆绑）→ 去掉版本。
2. **`java.util.Properties`** — `app/build.gradle.kts` 脚本里全限定名不可用 → 改用 `import java.util.Properties` + 裸 `Properties()`。
3. **WorkManager 不可用** — 当前 Maven 镜像缺失整个 `androidx.work` 分组（2.7.1/2.8.1/2.9.0 全 404，其余 androidx 正常）→ **弃用 WorkManager**，改用 `AlarmManager` 每日不精确闹钟：`RecycleBinScheduler`（RTC_WAKEUP，~03:00）+ `RecycleBinPurgeReceiver`（Hilt `@EntryPoint` 取 `RecycleBinRepository`，30 天保留，`goAsync()` 保活）。
4. **MainActivity/TickClearApplication 放错目录** — 原在 `app/`（不在源集，不会编译），已迁至 `app/src/main/java/com/tickclear/app/`。
5. **自适应图标** — `<adaptive-icon>` 需 API26+，minSdk 24 → 移到 `drawable-v26/ic_launcher.xml`，`drawable/ic_launcher.xml` 改为传统矢量回退。
6. **BootReceiver 无 intent-filter** — 补 `BOOT_COMPLETED` intent-filter + `exported="true"`。
7. **ConflictChecker 无法注入** — 是 Kotlin `object`，Hilt 无绑定 → `AppModule` 加 `@Provides`。
8. **扩展函数未导入** — `occursOn`/`effectiveStartMin` 在 `domain.conflict` 包，`GetTodayTasksUseCase`/`StatsUseCases` 需显式 import。
9. **suspend 调用越界** — `StatsUseCases.tryUnlock` 局部函数改 `suspend fun`。
10. **Compose 委托** — `TickClearNavGraph` 缺 `import androidx.compose.runtime.getValue`。
11. **私有色板** — `Color.kt` 的 `LightColorScheme/DarkColorScheme` 由 `private` 改 `internal`（跨文件访问）。
12. **SQLCipher 类名/依赖** — 代码用 `net.zetetic.database.sqlcipher.SupportFactory`，但该类属新 artifact 且类名为 `SupportOpenHelperFactory`。改依赖 `net.zetetic:sqlcipher-android:4.5.4`，import 改 `SupportOpenHelperFactory`，并在建库前 `System.loadLibrary("sqlcipher")`（该 artifact 不自动加载 native 库）。

## 关键约定（本次确认）
- WorkManager 在此环境不可用；周期任务一律走 AlarmManager。
- SQLCipher = `net.zetetic:sqlcipher-android`（非 legacy `android-database-sqlcipher`），需手动 `System.loadLibrary("sqlcipher")`，工厂类 `SupportOpenHelperFactory`。
- 构建命令：`./gradlew assembleDebug`（首次需联网下依赖）。

## Tab 顺序调整（用户要求）
- 底部 Tab 顺序改为：**今日 / 助手 / 任务 / 统计 / 设置**。仅改 `Routes.kt` 中 `topLevelDestinations` 列表顺序（`startDestination` 仍为 `today`）。
- 助手 Tab 定位明确：以「与小智语音/文字聊天」为主，对话中提及要新增的任务才通过 function calling（`create_task` 工具）自动落库。已同步到 `docs/开发计划_任务清单.md` Phase 6 描述。

## 待办
- P1.6 种子/模板初始化未做。
- Phase 2–6：五个 Tab 目前仍为占位屏。
- 单测（ConflictChecker/streak）与显式 Room 迁移延后 Phase 7。

## 整体审查 + TaskInstance 两层模型落地（用户要求"整体审查"后）
- **审查发现 3 个严重逻辑 bug**（已修）:
  - B1 INTERVAL 锚点错：`ScheduleUtils` 原 `anchor/1440` 整数除恒 0、`DAYS.between(epochDay0,...)` 从 1970 起算 → 改为 `repeatAnchorDate`(LocalDate) 起算 `DAYS.between(anchor,date)%interval`。
  - B2 一次性任务无日期：`TaskEntity` 加 `scheduledDate`(null=随时任务,每天生成)；`shouldGenerateInstance` 替代 `occursOn`。
  - B3 CompletionLog 非幂等：`CompletionLogEntity` 加唯一索引 `(taskId,dateLocal)` + DAO `insert` 改 `IGNORE`。
- **数据模型决策（用户拍板：按 PRD 补 TaskInstance）**:新增 `TaskInstanceEntity`(主键 `${taskId}@${date}`、唯一索引幂等)+ `TaskInstanceDao` + `TaskInstanceRepository`(`ensureInstancesForDate` 懒生成)；`CompleteTaskUseCase` 改为完成实例+写去重 CompletionLog(非重复任务才置 Task 终态)；`GetTodayTasksUseCase`/`GetStatsUseCase`/`CheckMedalsUseCase`/`RecordCheckInUseCase` 的"今日 done/total"改由当日实例派生；streak 抽 `StreakUtils`（修 A1 重复实现）。`AppDatabase` version 1→2（fallbackToDestructiveMigration，开发期可接受）。
- **M3 回收站恢复丢完成态**:`TaskDao.restore` 不再 `status=0`；`RecycleBinRepository.purgeExpired` 级联清实例。
- 文档同步:计划 `docs/开发计划_任务清单.md` 已补 TaskInstance 决策块、P1.1 改 6 实体、P3.4 改 Receiver/Scheduler。

## 仍未解决的审查项（留给后续 phase）
- M2 提醒系统未实现(`ReminderReceiver` 空实现、未调度；`SCHEDULE_EXACT_ALARM` 权限暂未用) —— Phase 2 必做。
- L1 `SecureStore` 存口令用 `apply()` 建议改 `commit()`(防首启丢库)；L2 `System.loadLibrary` 建议加守卫(当前 @Singleton 单次安全)。
- PRD 有但计划未列的:位置提醒、稍后提醒/通知栏完成/跳过、语音解析确认卡、随机鼓励 —— 建议显式标 Non-Goal 或补 phase。
- `exportSchema=true` 仅 warning(未配 schemaLocation),Phase 7 配 Room Gradle 插件或设 false。

## P1.6 + Phase 2 + M2 + L1/L2 全部落地（BUILD SUCCESSFUL，17:40 验证）

本次会话基于早先摘要继续，最终 `./gradlew assembleDebug` 通过（无错误、无 opt-in 警告）。

### 已完成代码
- **P1.6 种子** `SeedUseCase`(@Singleton)：`firstRunDone` 判定，首启注入 3 组（晨间/健康/工作）+ 8 任务（含 DAILY 重复与一次性今日任务、部分 `reminderEnabled=true`）。`TickClearApplication` 协程内 `seed.ensureSeeded()`。
- **Phase 2 今日 Tab 真实 UI**：`TodayScreen`(TopAppBar+ProgressRing+ConflictBanner+PullToRefreshBox+FAB+TaskEditSheet+Snackbar 撤销) / `TodayViewModel`(combine 任务+组+软删pending+鼓励语；complete/delete/undoDelete/saveTask/refreshEncouragement) / `TaskItem`(SwipeToDismissBox 右滑完成左滑软删) / `TaskEditSheet`(ModalBottomSheet+组/重复/级别/提前量+全天+TimePicker) / `ProgressRing` / `ConflictBanner`。`uiState` 通过 `GetTodayTasksUseCase`(flatMapLatest+flow 修正) 派生。
- **M2 提醒系统**：`ReminderScheduler`(AlarmManager `setExactAndAllowWhileIdle` 替代 WorkManager；`rescheduleAll`/`scheduleForTask`/`cancelForTask`/`scheduleSnooze`；API≥S 无 `SCHEDULE_EXACT_ALARM` 降级 `setAndAllowWhileIdle`) + `ReminderReceiver`(SHOW/COMPLETE/SNOOZE/SKIP 动作；完成走 `completeTaskUseCase`、跳过走 `instanceRepository.skip`) + `NotificationHelper`(reminder/high/mid/low 四渠道) + `BootReceiver`(开机重排) + `AppDatabase` version 2→3（exportSchema=false，fallbackToDestructiveMigration）。`TaskEntity` 增 `reminderOffsetMin`；`TaskInstanceDao` 增 `setSkipped`。
- **L1** `SecureStore` 写口令 `apply()`→`commit()`。**L2** `AppDatabase` 加 `synchronized` 守卫 `System.loadLibrary("sqlcipher")`（companion `sqlcipherLock`/`@Volatile sqlcipherLoaded`）。
- **PRD 编排**：计划文档文末追加「PRD 功能编排」表（提醒送达/通知完成/稍后/跳过/随机鼓励=已落地；位置提醒=v1.1 Non-Goal；语音确认卡=Phase 5/6；静音时段/组级定时/全屏提醒=后续）。

### 构建期修复的关键错误（最终 4 轮迭代）
1. **KAPT `NonExistentClass`**：`AppModule.kt` 内残留的 `@EntryPoint interface AppEntryPoint` 误用 `import dagger.EntryPoint`（应为 `dagger.hilt.EntryPoint`），KAPT 生成 stub 失败。→ 移到独立 `di/AppEntryPoint.kt`（正确 import），并清理 `AppModule.kt` 多余 import 与残留 interface。**经验：Kotlin `object @Module` 与 `@EntryPoint interface` 不可同文件（会触发 KAPT stub 异常）；普通 `class`+`@EntryPoint` 同文件可（RecycleBinEntryPoint 先例）。**
2. **`ReminderScheduler.rescheduleAll`**：`observeAll()` 的 `first()` 未 import `kotlinx.coroutines.flow.first` → 级联 `Unresolved 'first'/'it'/forEach 歧义`。→ 加 import，改 `tasks.first()`。
3. **`TaskItem`**：缺 `androidx.compose.ui.unit.dp` import（多处 `dp` 报错）；`groupColor` 调 `MaterialTheme`(Composable) 须标 `@Composable`。
4. **`TaskEditSheet`**：缺 `Box`/`clickable` import；`DropdownField` 用 `ExposedDropdownMenuBox`/`menuAnchor` 等实验 API 须 `@OptIn(ExperimentalMaterial3Api::class)`；`DropdownField` 的 `options: List<Pair<T,String>>`（label 已是 String）内部却 `stringResource(labelRes)` 类型错 → 改 `Text(label)`。
5. **`TodayScreen`**：`LaunchedEffect` 的 suspend lambda 内调 `stringResource`(Composable) 非法 → 将字符串提到 composable 作用域 `snackbarMsg`/`restoreLabel` 再传入。
6. **opt-in 警告**：`GetTodayTasksUseCase`/`StatsUseCases` 的 `flatMapLatest` 加 `@OptIn(ExperimentalCoroutinesApi::class)`。

### 当前状态
- 五个 Tab 中仅「今日」为真实 UI；助手/任务/统计/设置仍为占位（Phase 3-6 未做）。
- 残留 warning（仅弃用提示，不影响构建/运行）：`CircularProgressIndicator(progress=Float)` 与 `Modifier.menuAnchor()` 的 Material3 1.3.0 弃用重载。可后续按需迁移到 lambda/MenuAnchorType 重载。
- 下一步可从 Phase 3（任务 Tab + 回收站）继续。

---

## 续：Phase 7 打磨（P7.3 中文抽离 + P7.4 无障碍）+ 真实小智 WebSocket（P6.2）落地

### P7.3 中文抽离 strings.xml（已全量消除 Compose 内联中文）
- 扫描 `ui/**/*.kt`：`\p{Han}` 仅剩注释与 `StatsViewModel.computeTrend` 的 MONTH 轴标签 `"${d.monthValue}月"`。
- 该轴标签属用户可见文本 → 抽出：ViewModel 改输出裸数字 `"${d.monthValue}"`，`StatsScreen.TrendBars` 增加 `period: StatsPeriod` 参数，MONTH 分支用 `stringResource(R.string.stats_trend_month, ...)`（= "%1$d月"）渲染；`strings.xml` 新增 `stats_trend_month`。
- 其余 UI 文案此前已抽至 `strings.xml`（assistant/settings/about/heatmap/medal/progress/task/today/nav 等），本次仅补 `stats_trend_month` 与若干 a11y 字符串（见下）。

### P7.4 无障碍审计（contentDescription / mergeDescendants / Role.Button 全路径）
- 关键坑：`contentDescription` 与 `role` 是 `androidx.compose.ui.semantics` 包下的**扩展属性**，仅 import `semantics` 函数不够，必须在每个使用文件显式 `import androidx.compose.ui.semantics.contentDescription`（及 `role`）→ 否则 `Unresolved reference 'contentDescription'/'role'`。已为 AssistantScreen/HeatmapCalendar/MedalWall/ProgressRing/TaskItem/SettingsScreen/TasksScreen 补齐。
- 覆盖点：任务行 `TaskItem`、分组行 `TasksScreen.GroupHeaderRow/TaskRow`、滑动删除背景图标(`a11y_swipe_delete`)、热力图单元 `HeatmapCalendar`(`a11y_heatmap_cell`)、勋章格 `MedalWall`(`a11y_medal`)、完成率环 `ProgressRing`(`a11y_progress_ring`)、设置可点行 `SettingsScreen.ClickableRow`(`mergeDescendants`+`Role.Button`+`"$title，$subtitle"`)、连接状态芯片 `AssistantScreen.ConnectionChip`(`mergeDescendants`)。

### P6.2 真实小智 WebSocket 传输（替换原 Mock-only 出口）
- `domain/assistant/WebSocketXiaozhiTransport.kt`（新）：OkHttp `WebSocketListener` 对接 `wss://api.xiaozhi.me/ws`（endpoint 取自 `SettingsRepository.assistantEndpoint`，缺省回退默认地址）；`onOpen` 发 hello（携带 `audio_params` opus/16000/1/60 + `prompt` 人设 + 可选 `token`/Authorization 头）；`handleServerMessage` 解析 `hello`(取 session_id+欢迎)/`welcome`(拦截官方默认话术)/`stt`/`llm`/`mcp`/`goodbye|abort`；`mcp` 用 `CoroutineScope(SupervisorJob()+Dispatchers.IO)` 调 `mcpTools.handle(...)`(suspend) 并回执 `result`(JSON 字符串)，同时发 `McpToolResult` UI 事件（原错误：suspend 调用不在协程 / `jsonPrimitive` 未 import）。
- `domain/assistant/XiaozhiTransportRouter.kt`（新）：门面，按 `settings.assistantMode.first()` 在 `MockXiaozhiTransport`/`WebSocketXiaozhiTransport` 间切换，转发 `events` 给自身 `Flow`；对 ViewModel 透明。
- `di/AssistantModule.kt`：改 `provideXiaozhiTransport = XiaozhiTransportRouter(settings, mcpTools)`。
- `domain/assistant/XiaozhiModels.kt`：`XiaozhiEvent` 加 `McpToolResult(message, taskCreated)`；`ui/assistant/AssistantViewModel.kt` 的 `onEvent` 处理该事件追加 system 气泡。
- `data/repositories/SettingsRepository.kt`：加 `getAssistantToken(): String?`（读 `SecureStore` 的 `PREF_XZ_TOKEN`，`Dispatchers.IO`）；`ui/assistant/AssistantConfigSheet.kt` 令牌读写改用 `SettingsRepository.PREF_XZ_TOKEN`（原 `KEY_XZ_TOKEN` 常量已删，需补 `import com.tickclear.app.R`）。

### 构建与验证
- `./gradlew assembleDebug` → **BUILD SUCCESSFUL**（35s，46 任务）。业务代码零 deprecation 警告，仅 KAPT 工具链提示（Kotlin 2.0 + KAPT 语言版本回退 / 选项未识别），不影响运行。
- `docs/开发计划_任务清单.md`：P6.2 / P7.3 / P7.4 勾选 ✅。
- 五 Tab 进度：今日✅ / 任务✅ / 统计✅ / 助手✅(Mock+Real 可切) / 设置✅ 全部真实可用。

### 下一步（可选）
- P6.3 `OpusCodec`（真实音频模式）、P7.1 WindowSizeClass 双栏、P7.5 兼容性矩阵冒烟。
