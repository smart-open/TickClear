# 点清 TickClear · 开发计划 v2.5 任务清单

> 本文档归纳 **v2.4 封板后仍未完成 / 阻塞 / 本轮复查新发现** 的全部任务，作为 v2.5 开发的唯一任务总表。
> v2.4.0 已于 2026-07-25 封板（V2.40 不再提示 / V2.41 软删组恢复级联 / V2.42 离线热词 / V2.43 方言识别 + 封板前审查加固），详见 `docs/开发计划_v2.4_任务清单.md`、`docs/成熟度评估_v2.0.md`、`release-notes.md`。
>
> **范围来源（三类）**：
> 1. v2.4 计划中的真机/基准项（V2.44–V2.51），无法在当前环境验证，结转；
> 2. v2.4 封板前代码复查发现的 P2 级问题（当前可开发，列于「一」）；
> 3. 各维度优化候选（列于「二」「三」）。

---

## 一、代码质量与健壮性（当前可开发，零新依赖优先）

> ✅ **本节 7 项已于 2026-07-24 全部完成**，通过 `./gradlew :app:testDebugUnitTest :app:lintRelease`（单测全绿、lint 0 error）。

- [x] **V2.52 · 通知 id 稳定化（防 hashCode 碰撞覆盖）** ✅
  - 实现：新建 `ReminderIds`（FNV-1a 32 位，非负），通知 id / content / complete / snooze / skip / fullScreen 各用独立前缀隔离；`ReminderScheduler.cancelForTask` 额外撤销旧 `hashCode()` 版 PendingIntent，兼容 v2.4.0 遗留闹钟。
  - 验收：`ReminderIdsTest` 4 用例（同 id 多动作互异 / 不同 id 互异 / 确定性 / 非负）全绿。

- [x] **V2.53 · 清理遗留死代码 `asrType/llmType`** ✅（范围修正）
  - 复查发现 `aiMode` 在 `SettingsScreen`（AI 引擎选择器）、`DebugViewModel/DebugScreen`、`SettingsViewModel` 仍有活引用，**保留**；仅 `asrType/llmType` 为真死代码。
  - 实现：删除接口与实现中的 `asrType/llmType` Flow、setter 及 `KEY_ASR/KEY_LLM`。验收：grep 无残留引用；`lintRelease` 通过。

- [x] **V2.54 · TodayScreen 键盘焦点跨 active/done 段错位** ✅
  - 实现：`focusedIndex` 仅在 active 段内计算，active 段改 `itemsIndexed` 按段内索引高亮，done 段恒不高亮；`↑↓` 索引夹取 `[0, count-1]`、`scrollToItem(1+idx)` 补偿头部；顺带修复原 `DPAD_UP` 从不更新 `focusedIndex` 的固有 bug。

- [x] **V2.55 · TodayScreen 编辑态软删后误建任务防护** ✅
  - 实现：`LaunchedEffect(editingTaskId, showEditor)` 检测编辑中任务从列表消失时直接关闭编辑页，不再以 `initial=null` 回退成「新建」。

- [x] **V2.56 · AppDatabase 迁移与导出 schema 一致性核验** ✅
  - 核验：`app/schemas/.../5.json` 为唯一导出 schema；空迁移 1→2、2→3 与 v5 实体字段一致，无需补正。
  - 发现并修复：`MIGRATION_4_5` 漏建 v5 实体声明的单列索引 `index_task_instance_dueDateLocal`（老库升级将触发 Room 校验崩溃），已补 `CREATE INDEX IF NOT EXISTS`，DROP/CREATE 全幂等。

- [x] **V2.57 · AssistantViewModel 消息无上限内存控制** ✅
  - 实现：`append` 保留最近 `MAX_MESSAGES = 100` 条（`takeLast`），超长会话内存不再单调增长。

- [x] **V2.58 · AudioCapture 长录音 PCM 边录边写** ✅
  - 实现：`startAccumulate` 改为边录边写 `cacheDir/asr_upload.pcm` 临时文件；`WavUtil.writePcmFromFile` 8KB 缓冲流式转 WAV；上传后删除 wav 与 pcm 临时文件。峰值内存从约 2× 全量 PCM 降至单个音频缓冲区。

---

## 二、验收指标评测（真机 / 基准，结转自 v2.4）

> 🔶 已知限制：以下依赖物理设备 / `connectedAndroidTest` 模拟器，当前环境无法执行，结转 v2.5 真机轮次。

- [ ] **V2.44 · 提醒送达率 ≥ 98%（含后台被杀）** 🔶 待真机评测
- [ ] **V2.45 · 性能基准：动画 60fps、冷启动 < 500ms** 🔶 待 Macrobenchmark/模拟器
- [ ] **V2.46 · 语音指标评测集** 🔶 待真机语音评测集（意图 ≥92% / 槽位 ≥88% / 耗时 <4s / 误删 <5%）

## 三、全量冒烟 QA 收口（真机人工，结转自 v2.4）

> 🔶 已知限制：人工真机走查，结转 v2.5。

- [ ] **V2.47 · 核心流程冒烟** 🔶 待真机
- [ ] **V2.48 · 自适应布局 QA** 🔶 待真机/折叠屏
- [ ] **V2.49 · 语音流水线 QA** 🔶 待真机
- [ ] **V2.50 · 无障碍 QA** 🔶 待真机/TalkBack
- [ ] **V2.51 · 兼容与降级 QA** 🔶 待真机/OEM

---

## 四、封板要求（v2.5.0）

- ✅ 一 中**当前可开发项（V2.52–V2.58）** 已全部落地并通过 `lintRelease` 0 error + `testDebugUnitTest` 全绿（2026-07-24）。
- 🔶 真机项（V2.44–V2.51）待真机轮次，结论届时写入 `release-notes.md`，未达标项标注已知限制（v2.5.0 以已知限制封板）。
- ✅ 按功能拆分 git commit（`[fix]` 5 个 + `[docs]` + `[config]`）；打 annotated tag `v2.5.0`。
- ✅ 红线守住：零新依赖；中文全抽离 `strings.xml`；Room 显式 Migration；`.workbuddy/` 不提交。
