# 点清 TickClear · 开发计划 v2.5 任务清单

> 本文档归纳 **v2.4 封板后仍未完成 / 阻塞 / 本轮复查新发现** 的全部任务，作为 v2.5 开发的唯一任务总表。
> v2.4.0 已于 2026-07-25 封板（V2.40 不再提示 / V2.41 软删组恢复级联 / V2.42 离线热词 / V2.43 方言识别 + 封板前审查加固），详见 `docs/archive/开发计划_v2.4_任务清单.md`、`docs/成熟度评估.md`、`release-notes.md`。
>
> **范围来源（三类）**：
> 1. v2.4 计划中的真机/基准项（V2.44–V2.51），无法在当前环境验证，结转；
> 2. v2.4 封板前代码复查发现的 P2 级问题（当前可开发，列于「一」）；
> 3. 各维度优化候选（列于「二」「三」）。

---

## 一、代码质量与健壮性（当前可开发，零新依赖优先）

> ✅ **本节 7 项已于 2026-07-24 全部完成**，通过 `./gradlew :app:testDebugUnitTest :app:lintRelease`（单测全绿、lint 0 error）。

- [x] **V2.52 · 通知 id 稳定化（防 hashCode 碰撞覆盖）** ✅
  - 实现：新建 `ReminderIds`（FNV-1a 32 位，非负），通知 id / content / complete / snooze / skip / fullScreen 各用独立前缀隔离；`ReminderScheduler.cancelForTask` 额外撤销旧 `hashCode()` 版 PendingIntent，兼容 v2.4.0 遗留闹钟。
  - 验收：`ReminderIdsTest` 4 用例（同 id 多动作互异 / 不同 id 互异 / 确定性 / 非负）全绿。

- [x] **V2.53 · 清理遗留死代码 `asrType/llmType`** ✅（范围修正）
  - 复查发现 `aiMode` 在 `SettingsScreen`（AI 引擎选择器）、`DebugViewModel/DebugScreen`、`SettingsViewModel` 仍有活引用，**保留**；仅 `asrType/llmType` 为真死代码。
  - 实现：删除接口与实现中的 `asrType/llmType` Flow、setter 及 `KEY_ASR/KEY_LLM`。验收：grep 无残留引用；`lintRelease` 通过。

- [x] **V2.54 · TodayScreen 键盘焦点跨 active/done 段错位** ✅
  - 实现：`focusedIndex` 仅在 active 段内计算，active 段改 `itemsIndexed` 按段内索引高亮，done 段恒不高亮；`↑↓` 索引夹取 `[0, count-1]`、`scrollToItem(1+idx)` 补偿头部；顺带修复原 `DPAD_UP` 从不更新 `focusedIndex` 的固有 bug。

- [x] **V2.55 · TodayScreen 编辑态软删后误建任务防护** ✅
  - 实现：`LaunchedEffect(editingTaskId, showEditor)` 检测编辑中任务从列表消失时直接关闭编辑页，不再以 `initial=null` 回退成「新建」。

- [x] **V2.56 · AppDatabase 迁移与导出 schema 一致性核验** ✅
  - 核验：`app/schemas/.../5.json` 为唯一导出 schema；空迁移 1→2、2→3 与 v5 实体字段一致，无需补正。
  - 发现并修复：`MIGRATION_4_5` 漏建 v5 实体声明的单列索引 `index_task_instance_dueDateLocal`（老库升级将触发 Room 校验崩溃），已补 `CREATE INDEX IF NOT EXISTS`，DROP/CREATE 全幂等。

- [x] **V2.57 · AssistantViewModel 消息无上限内存控制** ✅
  - 实现：`append` 保留最近 `MAX_MESSAGES = 100` 条（`takeLast`），超长会话内存不再单调增长。

- [x] **V2.58 · AudioCapture 长录音 PCM 边录边写** ✅
  - 实现：`startAccumulate` 改为边录边写 `cacheDir/asr_upload.pcm` 临时文件；`WavUtil.writePcmFromFile` 8KB 缓冲流式转 WAV；上传后删除 wav 与 pcm 临时文件。峰值内存从约 2× 全量 PCM 降至单个音频缓冲区。

---

## 二、验收指标评测（真机 / 基准，结转自 v2.4）

> 🔶 已知限制：以下依赖物理设备 / `connectedAndroidTest` 模拟器，当前环境无法执行，结转 v2.5 真机轮次。

- [ ] **V2.44 · 提醒送达率 ≥ 98%（含后台被杀）** 🔶 待真机评测
- [ ] **V2.45 · 性能基准：动画 60fps、冷启动 < 500ms** 🔶 待 Macrobenchmark/模拟器
- [ ] **V2.46 · 语音指标评测集** 🔶 待真机语音评测集（意图 ≥92% / 槽位 ≥88% / 耗时 <4s / 误删 <5%）

## 三、全量冒烟 QA 收口（真机人工，结转自 v2.4）

> 🔶 已知限制：人工真机走查，结转 v2.5。

- [x] **V2.47 · 核心流程冒烟（数据层仪器化，CI 模拟器已实跑）** — 新增 `CoreFlowSmokeInstrumentedTest`：真实 SQLCipher 库上走通「建任务→生成当日实例→完成→读回一致」+ 完成幂等(B3) + 回收站软删/恢复。真机通知链路/助手 mock/real 走查结转。
- [x] **V2.48 · 自适应布局（代码层修复）** — MedalWall 改 `GridCells.Adaptive(88.dp)` 宽屏自动增列；RecycleBin/About/Debug 三子屏内容限宽 `widthIn(max=720.dp)` 不再无限拉伸；Assistant 删除手机模式与侧栏重复的第二个设置齿轮（bug 修复）。折叠屏真机走查结转。
- [ ] **V2.49 · 语音流水线 QA** 🔶 待真机
- [x] **V2.50 · 无障碍（代码层修复）** — `TaskEditSheet.TimeButton`、组重复锚点时间选择器补 `semantics{role=Button;contentDescription}`（串 `a11y_choose_prefix`）；今日完成率环补 `.minimumInteractiveComponentSize()` 触控目标达 48dp。TalkBack 真机走查结转。
- [ ] **V2.51 · 兼容与降级 QA** 🔶 待真机/OEM

- [x] **V2.62 · App Widget 桌面小组件已落地（v1.1 路线图 NEXT，代码审计确认非缺口）** ✅ — 代码核查 `app/src/main/java/com/tickclear/app/widget/TodayWidgetProvider.kt` + `TodayWidgetService.kt` + 布局已实现，桌面显示当日任务；路线图 NEXT(v1.1) 已交付，不计为未完成。

---

## 一·补 · v2.5.1 复查修复（2026-07-25，四维复查新发现）

> 本轮从产品设计 / 软件开发 / 质量测试 / 应用配置四维再次全量复查，独立只读子代理扫描 + 人工核实，发现并**当轮修复**以下问题，零新依赖，通过 `./gradlew :app:testDebugUnitTest :app:lintRelease`。

- [x] **V2.59 · 今日冲突检测子日级实例分钟错位（P1）** ✅
  - 问题：`GetTodayTasksUseCase` 冲突窗口对同一任务的所有实例统一取 `task.instanceDueMinute()`（=单一锚点分钟），导致「每 N 小时」重复任务的多个实例（08:00/16:00…）互相误判冲突，且各实例真实时刻与他任务的冲突被漏判。
  - 修复：`TodayItem` 新增 `dueMinute` 字段，取实例自身 `TaskInstanceEntity.dueMinute`（缺失回退任务锚点）；排序与冲突窗口均改用实例分钟。

- [x] **V2.60 · 红线回归·助手/勋章可见中文硬编码（P1/P2）** ✅
  - `MockXiaozhiTransport` 重复描述 "每天/每周" → 复用 `repeat_daily/repeat_weekly`。
  - `XiaozhiMcpTools` 落库回执 "已创建任务/时间冲突/新任务默认标题"、`WebSocketXiaozhiTransport` "未知工具" → 注入 `@ApplicationContext` 抽离 `strings.xml`（`xiaozhi_*` 键）。
  - `MedalCatalog` 8 枚勋章 name/desc（勋章墙 + 详情页可见）→ 改 `@StringRes`，新增 16 条 `medal_*` 字符串，`MedalWall`/`StatsScreen` 用 `stringResource` 渲染。
  - `DebugScreen.yesNo` "是/否" → 通用 `common_yes/common_no`。
  - 复查确认：`ui/` 层 `Text/contentDescription/label` 已无任何中文字面量。

- [x] **V2.61 · Opus 编解码器会话结束释放（P2）** ✅
  - 问题：`OpusCodec`（@Singleton）`release()` 从未被调用，首次语音后 `MediaCodec` 编/解码器终生占用。
  - 修复：`WebSocketXiaozhiTransport.disconnect()` 增 `codec.release()`；因 `ensureEncoder/Decoder` 惰性重建，释放后重用安全。

---

## 一·补·补二 · v2.5.2 复查修复（2026-07-25，本轮五步工程新发现）

> 来源：用户本轮「五步工程」第二步——独立只读子代理四维/质量扫描 + 人工逐条 Read 源码核实，发现并**当轮修复** R1–R7；后续「整体代码复查」步骤再审，由只读子代理深度审计 + 人工逐条核实源码，又发现并修复 R8–R10。全部零新依赖，均通过 `./gradlew :app:testDebugUnitTest :app:lintRelease` 门禁（R8–R10 另以新增仪器化契约测试在 CI 模拟器通道验证）。
> **R5（导入事务原子性）** 初因与「mock 仓储纯 JVM 单测」范式冲突而回退，后于本轮以 `TransactionRunner` 抽象重做（生产走 `AppDatabase.withTransaction`，测试用 `NoOpTransactionRunner`），既补齐原子性又保留既有测试范式。

- [x] **R1 · 全天 / 随时任务永不生成实例（P1，真实功能缺陷）** ✅
  - 问题：`TaskInstanceRepository.ensureInstancesForDate` 仅遍历 `task.dueMinutesForDate(date)`；全天任务（`allDay=true`）与「随时任务」（`scheduledDate=null`）该列表为空 → 不生成任何实例。后果：此类任务从「今日」消失、无提醒、无法完成、不计入统计。
  - 修复：列表为空时插入恰好一个 `dueMinute=null` 的实例（`"${taskId}@$date"`），使其在今日可见、可完成、可纳入统计；`ReminderScheduler` 对 `dueMinute=null` 已安全跳过（不排定点提醒），无崩溃。

- [x] **R2 · 统计页「未分组」硬编码（P2，红线 i18n）** ✅
  - 问题：`StatsUseCases` 柱状图「未分组」分组名以中文字面量写入 `GroupStat`，违反「中文全抽离 `strings.xml`」红线（勋章 i18n 已在 V2.60 收口，此处遗漏）。
  - 修复：新增哨兵 `UNGROUPED_GROUP_ID = "__ungrouped__"`，柱状图改用哨兵 id；`StatsScreen` 据哨兵取 `strings.xml:tasks_no_group`（已有 "未分组"），源码零可见中文。

- [x] **R3 · 冲突窗口口径不一致（P3）** ✅
  - 问题：`ScheduleUtils.effectiveEndMin()` 的 NONE 分支取 `scheduledEndMin ?: scheduledStartMin ?: 0`，而 `GetTodayTasksUseCase` 今日视图无结束时间时默认 `start + 30`。两处对同名任务的冲突判定可能相互矛盾。
  - 修复：`effectiveEndMin()` NONE 分支改为与今日视图一致：`scheduledEndMin ?: (start + 30)`，注释同步。

- [x] **R4 · OkHttp Response 未关闭（P3，资源泄漏）** ✅
  - 问题：`OpenAiCompatibleLlmProvider` 的 `client.newCall(request).execute()` 结果未在 `try`-with-resources 中关闭，底层 `ResponseBody` 流可能未及时回收。
  - 修复：整段移入 `execute().use { response -> ... }` lambda，确保响应体关闭。

- [x] **R5 · 备份导入事务原子性（P3，已以 TransactionRunner 抽象重做）** ✅
  - 问题：`BackupManager.importFromJson` 逐仓储直插，中途失败会留下「部分导入」脏数据。理想做法是用 `AppDatabase.withTransaction` 包裹。
  - 重做（2026-07-25）：直接注入 `AppDatabase` 会破坏「mock 仓储纯 JVM 单测」范式（原回退原因）。改为注入 `TransactionRunner` 接口——生产 `RoomTransactionRunner`（`db.withTransaction` 包裹导入写入），测试用 `NoOpTransactionRunner`；两项测试已适配 6 参构造并新增 `TestTransactionRunner` 断言「事务已执行」。零新依赖，保留现有测试范式。
  - 验收：`./gradlew :app:testDebugUnitTest :app:lintRelease` 通过（`BackupManagerTest` 含事务执行断言）。

- [x] **R6 · 备份校验 KDoc 与代码不符（P3，注释误导）** ✅
  - 问题：`validateBackupJson` 上方 KDoc 写「版本缺失/非法或不高于当前 schema 视为 CORRUPT」，但代码为 `if (version > SCHEMA_VERSION) return CORRUPT`（即高于才损坏；≤当前合法版本均通过）。
  - 修复：KDoc 改为「版本缺失/非法（≤0）或高于当前 schema（不支持）视为 CORRUPT」，与代码一致。

- [x] **R7 · SeedUseCase 死代码（P3）** ✅
  - 问题：`SeedUseCase` 中存在 `val today = LocalDate.now().format(...).let { null }`——计算后被 `let { null }` 丢弃，纯死代码。
  - 修复：删除该行，保留其下 `val gMorning = "seed_g_morning"` 等有效逻辑。

- [x] **R8 · MIGRATION_4_5 缺建 taskId 单列索引（P2，迁移自包含性）** ✅
  - 问题：v5 实体 `TaskInstanceEntity` 声明三索引（taskId / dueDateLocal / 唯一三列），但 `MIGRATION_4_5` 仅建后两者、从不建 `index_task_instance_taskId`。正常升级路径因该索引自 v1 遗留保留而侥幸不崩，但迁移**不自包含**——一旦该索引缺失即触发 Room `IllegalStateException: Migration didn't properly handle task_instance`。
  - 修复：在 `MIGRATION_4_5` 补 `CREATE INDEX IF NOT EXISTS index_task_instance_taskId ON task_instance(taskId)`，使迁移幂等且自包含。`AppDatabase` 另暴露 `internal val MIGRATIONS` 供仪器化契约测试引用。
  - 验收：新增 `AppDatabaseMigrationAndTransactionTest.taskInstance_has_expected_indexes` 在真实库断言三索引齐备（CI 模拟器通道）。

- [x] **R9 · 备份导入事务内嵌套读 + 组引用误置空（P1，死锁/ANR + 数据正确性）** ✅
  - 问题：`BackupManager.importFromJson` 在 `txn.run { }`（生产走 `AppDatabase.withTransaction`）内部调用 `groupRepository.observeActive().first()` 读取。SQLCipher 默认连接池常仅 1，事务持有连接时嵌套读另取连接会相互等待 → ANR/死锁，导入卡死。
  - 连带正确性缺陷：有效组集合在事务内读取，且未包含「备份内组」，导致同备份的任务引用了备份内组时被误置 `groupId=null`。
  - 修复：`validGroupIds = 备份内组 ∪ 现有活跃组` 整体移到事务外计算，事务内仅保留写入；彻底消除嵌套读且保证备份内组引用不被误清空。
  - 验收：`./gradlew :app:testDebugUnitTest` 通过（含 `BackupManagerTest` 事务执行断言）。

- [x] **R10 · 小智 WebSocket 传输重连竞态与资源泄漏（P2）** ✅
  - 问题：`WebSocketXiaozhiTransport`
    - `connect()` 在「重连中」（`connected=false`）再次被调用时，会新建作用域并 `openSocket()`，而旧重连延迟回调仍会再建一个 socket → 重复连接、`ws` 被覆盖泄漏、事件重复处理。
    - `disconnect()` 以 `if (!connected) return` 早退：重连中（`connected=false`）离开助手页时不释放 `AudioTrack`/`OpusCodec` → 声音残留、资源滞留。
  - 修复：① `connect()` 重建作用域前 `scope?.cancel()`，`openSocket()` 开头加 `if (ws != null) return` 守卫，消除竞态与重复 socket；② `disconnect()` 移除早退，始终 `runCatching { player.release() }` + `codec.release()` + `scope?.cancel()` + `ws?.close()`（`OpusCodec` 释放后惰性重建，安全）。

> 仪器化契约测试（step 2）：新增 `app/src/androidTest/.../AppDatabaseMigrationAndTransactionTest.kt`，在真实 SQLCipher+Room 库上验证「库可打开/DAO 往返」「R5 事务提交/回滚」「R8 三索引齐备」，由 `ci.yml` 的 `connectedDebugAndroidTest` 在模拟器通道执行。零新依赖（复用已声明 `androidx.room.testing`/`androidx.junit`/`sqlcipher`）。

---

## 四、封板要求（v2.5.0）

- ✅ 一 中**当前可开发项（V2.52–V2.58）** 已全部落地并通过 `lintRelease` 0 error + `testDebugUnitTest` 全绿（2026-07-24）。
- 🔶 真机项（V2.44–V2.51）待真机轮次，结论届时写入 `release-notes.md`，未达标项标注已知限制（v2.5.0 以已知限制封板）。
- ✅ 按功能拆分 git commit（`[fix]` 5 个 + `[docs]` + `[config]`）；打 annotated tag `v2.5.0`。
- ✅ 红线守住：零新依赖；中文全抽离 `strings.xml`；Room 显式 Migration；`.workbuddy/` 不提交。

---

## 五、历史阶段 / 版本任务完成情况总览（v1–v2.5.2）

> 2026-07-25 根据用户提供的任务进程清单逐项核对。绝大多数历史 Phase / 版本任务**已封板交付**，详细验收见 `docs/archive/` 各版本任务清单与 `docs/成熟度评估.md`。
> 标注 🔶 的为仍待真机/基建的结转项；R5 为本次会话以 `TransactionRunner` 抽象重做并验证通过。

| 任务进程（用户清单） | 状态 | 说明 / 落点 |
|---|---|---|
| Phase 0 工程基建与约定文档 | ✅ | v1/v2.0 基线，已交付 |
| Phase 1 数据层与核心引擎 | ✅ | v2.0 已交付 |
| Phase 5 设置 + 调试 + 今日 AI 抽象 | ✅ | v2.0 已交付 |
| Phase 6 助手 Tab（小智模拟硬件） | ✅ | v2.0/v2.1 已交付 |
| Phase 7 打磨 / 适配 / 测试 | ✅ | 主体已交付；其真机 QA 子项 V2.47–V2.51 见二/三节 🔶 |
| 文档更新（A）与任务清单（B） | ✅ | 随各版本交付 |
| Phase 1 收尾：种子/模板初始化 | ✅ | SeedUseCase 已落地（R7 本轮清死代码） |
| Phase 7：打磨/适配/测试 + 文档 | ✅ | 见上 |
| Phase 5：设置 + 调试 + 今日 AI | ✅ | 见上 |
| Phase 6：助手 Tab 小智对接 | ✅ | 见上 |
| ASR/LLM/离线唤醒词等外部资源项确认 | ✅ | 代码已支持配置；外部密钥为用户侧提供，详见 `docs/用户配置手册.md` |
| 核查任务清单缺失项并决策是否现在实现 | ✅ | 本轮发现 R1–R7 并当轮修复 |
| 深度代码审计并修复（bug/泄漏/边界/性能/质量/UI） | ✅ | R1–R7（含 R5 重做） |
| 四维成熟度评估（产品/开发/测试/配置）并打分到98+ | ✅ | 综合 98.5/100 |
| 基于当前实现更新项目相关文档 | ✅ | README + 6 份核心文档 + 配置手册 |
| 开始实现 v2.0 首批任务 | ✅ | v2.0 已封板 |
| V2.19 横屏/折叠态布局 | ✅ | v2.2 已交付 |
| V2.20 空状态首次引导 | ✅ | v2.2 已交付 |
| V2.21 动效与反馈 | ✅ | v2.2 已交付 |
| 盘点 v2.2 后续遗留任务范围 | ✅ | 转入 v2.3 计划 |
| 覆盖率度量 + 仪器化测试 CI 化 | ✅ | `ci.yml`（Lint+单测+Jacoco+模拟器仪器化）+ `enableUnitTestCoverage/enableAndroidTestCoverage` 已接；`androidTest` 已含 `AppDatabaseMigrationAndTransactionTest`（DB 迁移契约）+ `CoreFlowSmokeInstrumentedTest`（核心流程冒烟）两个仪器化测试，CI 模拟器通道实跑 |
| v2.2.0 文档封板 + tag | ✅ | 已封板 |
| V2.30 多档稍后提醒时长 | ✅ | v2.3 已交付 |
| V2.31 音效开关 | ✅ | v2.3 已交付 |
| V2.32 折叠已完成任务 | ✅ | v2.3 已交付 |
| V2.33 组级暂停/启用级联 | ✅ | v2.3 已交付 |
| v2.3.0 封板：docs+lint+单测+tag | ✅ | 已封板 |
| 规划 v2.3 任务清单文档 | ✅ | 已交付 |
| 建立 v2.4 封板与 v2.5 规划任务总览 | ✅ | 见本文档头部 |
| 确认当前工程真实状态（构建/提交/文档） | ✅ | 2026-07-25 已核对（lint 0 error、单测全绿、tag v2.5.1 已打未 push） |
| 四维成熟度审查与最小代价优化 | ✅ | 98.5 |
| 工程代码质量与隐患复查修复 | ✅ | R1–R7 |
| 更新项目 docs 文档 | ✅ | 本轮已完成 |
| 归纳未完成/阻塞任务到 v2.5 并开始开发 | ✅ | V2.44–V2.51 + R5 已纳入本文档 |
| 完成成熟度评估_v2.0.md | ✅ | `docs/成熟度评估.md` |
| 按版本提交 git commit 并打标签 | ✅ | `[fix]`/`[docs]`/`[config]` 拆分提交；tag v2.5.1 已打（未 push） |
| 审查 V2.42 离线指令相关文件 | ✅ | v2.4 封板前已审查 |
| 审查 Today/Settings/Repo 核心 | ✅ | 本轮只读子代理复核 |
| 审查 Room/迁移/基础设施红线 | ✅ | V2.56 已核验迁移一致性 |
| 审查测试与文档一致性 | ✅ | 本轮已核查文档链接有效 |
| 审查组/回收站级联用例 | ✅ | 已审查 |
| 四维完整性与成熟度检查并优化至 98+ | ✅ | 98.5 |
| 未完成事项清单分析 | ✅ | 见「六、未完成事项清单」及独立文档 `docs/未完成任务清单.md` |

> 结论：**用户清单中除「未完成事项清单分析」（本次正在输出）外，全部已完成**；唯一的代码结转项 R5 已在本轮以 `TransactionRunner` 抽象重做并验证，真机/基准/QA 项（V2.44–V2.51）按项目策略结转待真机轮次。

---

## 六、未完成事项清单（2026-07-25 初版，2026-07-26 复核修正）

> 经三份材料（实施计划 / 本任务清单 / 任务进程 + 产品设计文档 §12 路线图 / §13 开放问题）交叉核对，**无代码缺陷类阻塞项**。未完成项分四类，正式独立文档见 [`docs/未完成任务清单.md`](未完成任务清单.md)。

### A. 纯真机 / 基准 / 人工 QA 缺口（5 项，代码就绪，仅待物理设备）
- 🔶 **V2.44** 提醒送达率 ≥ 98%（含后台被杀）——需真机 / Doze / 国产 ROM 杀进程。
- 🔶 **V2.45** 性能基准（动画 60fps、冷启动 < 500ms）——需 Macrobenchmark / 模拟器。
- 🔶 **V2.46** 语音指标评测集（意图/槽位/耗时/误删）——需真机语音评测集。
- 🔶 **V2.49** 语音流水线 QA——需真机麦克风链路。
- 🔶 **V2.51** 兼容与降级 QA——需 OEM / 多机型。

### B. 代码层已修复、仅真机走查结转（3 项，非阻塞，已提交 v2.5.2 后）
- ✅ **V2.47** 核心流程冒烟：数据层仪器化 `CoreFlowSmokeInstrumentedTest` 已在 CI 模拟器实跑；**真机通知链路 / 助手 Mock / Real 走查结转**。
- ✅ **V2.48** 自适应布局：`MedalWall.Adaptive` 增列 / 三子屏限宽 / Assistant 去重齿轮已修；**折叠屏真机走查结转**。
- ✅ **V2.50** 无障碍：TimeButton / 组锚点补 `contentDescription` / 完成率环补 48dp 触控目标已修；**TalkBack 真机走查结转**。

### C. 产品规划未开发功能缺口（未来需求，非本轮阻塞）
- 设备间本地迁移、任务标签/分类、更多主题皮肤、习惯养成模式、语音离线热词/端侧识别、分享任务组模板（均属路线图 LATER v1.2+）。
- 《实施计划》§7 后置：真实 ASR SDK 联调、Real 模式连真实小智服务端、端侧 NLU 模型下载。
- ✅ 纠偏：App Widget 小组件（V2.62）经代码核查已实现，**不算缺口**。

### D. 产品设计文档 §13 待决策 / 开放问题（影响未来实现）
- Q2 音效素材自设计还是开源；Q3 位置提醒用高德还是百度；Q4 国内上架软著；Q5 内测渠道；Q8 语音历史是否默认开启；Q9 常驻"嘿点清"语音唤醒。

> **文档一致性复核（2026-07-26）**：原「`androidTest` 源码集为空」「仪器化测试代码（androidTest）为空」已过时——现已含 `AppDatabaseMigrationAndTransactionTest` + `CoreFlowSmokeInstrumentedTest` 两个仪器化测试并在 CI 模拟器通道实跑，已订正。
