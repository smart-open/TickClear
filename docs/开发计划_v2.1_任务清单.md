# 点清 TickClear · 开发计划 v2.1 任务清单

> 本文档为 **v2.0.0 封板后（2026-07-24 起）的下一阶段规划**，聚焦「质量加固与数据韧性」。
> 规则：完成一项即把 `- [ ]` 改为 `✅ V2.x ...`（✅ 置于任务前）并填写验收结果。
> v2.0 封板见 `开发计划_v2.0_任务清单.md`；发布说明见 `../release-notes.md`；项目规范见 `AGENT.md`。

---

## 0. 未完成规划分析（从哪里来）

v2.0 任务总表（V2.1–V2.21，共 21 项）**已全部交付**，并无独立的 v2.1 计划文档。为界定 v2.1 范围，对仓库内所有「未勾选 `- [ ]`」规划项做了一次归类审计，得到四类来源：

1. **显式不实现（负资产，维持不实现）** — `docs/开发计划_任务清单.md`（v1.0 主计划）仍挂 `- [ ]` 的 P5.3 `LocalRuleNluEngine`、P5.6 `VoicePanel/AiViewModel/...`：已在 v1.0/v2.0 复核为「被助手 Tab 等价覆盖 / 重复实现属负资产」，维持不实现，不计入 v2.1。
2. **手动 QA 冒烟清单** — `docs/冒烟测试清单_P7.5.md` 的 `- [ ]` 是人工设备测试项（需真机/模拟器），本环境无法执行；其中可自动化的部分已尽量沉淀为 JVM 单测（本次补 ICS 往返 / NLU / 备份健康）。
3. **设计文档漂移项** — `docs/点清APP_产品设计文档.md` 大量 `- [ ]` 实为**已实现但未勾选**（冲突红色警示条、空状态引导、时段问候语、已完成下沉半透明、稍后提醒 5/15/30、通知栏直接完成、静音时段、一键清空确认、动效 60fps 等）。属文档同步问题，非开发缺口；本版不逐条回填，避免误标。
4. **真实 v2.1 开发项** — 来自 release-notes「已知限制」+ 成熟度评估「后续建议」：仪器化测试仅编译验证、关键纯逻辑缺单测、备份无自愈校验。这构成 v2.1 的实质性范围。

> **工程纪律（沿用红线）**：中文全抽离 `strings.xml`、Room 显式 `Migration` 禁 `fallbackToDestructiveMigration`、**不引入新依赖**（测试依赖 JUnit/coroutines-test/turbine/mockk/org.json 已于 v2.0 声明，零新增）、按功能拆 `git commit`、提交前缀 `[fix]/[feature]/[test]/[docs]/[config]/[chore]`。

---

## Phase 7 · 质量与韧性加固（v2.1）

- [x] **V2.22** JVM 单元测试补全（可运行验证）：为 v2.0 新增强的纯逻辑模块补 JVM 单测 —— `IcsManager` RFC5545 往返（一次性/全天/日/周/月重复、特殊字符转义、非法输入容错、UID 保留）、`BackupManager.validateBackupJson` 备份健康判定（OK/CORRUPT/EMPTY）。`TaskIntentParser` 的 V2.15 口语规则用例已由既有 `TaskIntentParserTest` 覆盖，无需重复。`./gradlew testDebugUnitTest` 全绿可验证（零新依赖）。
- [x] **V2.23** 备份自愈校验（Backup Health）：`BackupManager` 新增纯函数 `validateBackupJson(json): BackupHealth`（解析+版本+结构校验，JVM 可测）；`AutoBackupRunner` 写入后即时解密校验并回写 DataStore 健康状态；`SettingsRepository` 新增 `lastBackupHealth`；`SettingsViewModel` 暴露；`SettingsScreen` 在自动备份区回显「上次备份状态：正常/损坏/空/未检查」，损坏用错误色提示重备。零依赖。
- [x] **V2.24** 文档与封板：新建本计划；`release-notes.md` 追加 v2.1.0 段；`开发计划_v2.0_任务清单.md` 封板段补 v2.1 链接；版本号升至 2.1.0 并打 annotated tag `v2.1.0`。

### 附：本轮顺手修复的 2 个 ICS 解析 bug（属 V2.22 回归屏障）
- `IcsManager.parseEvent`：按 `BEGIN:VEVENT` 拆分后首行带前导 `\r`，导致 `UID:`（即任务 id）被静默丢弃 → 归一化换行修复。
- 同上：全天判断误用值（`20260724`）而非 `DTSTART` 的 `VALUE=DATE` 参数，导致全天任务导入后丢失 `allDay` 标志 → 改从行参数判断。

---

## 验收与里程碑

- **v2.1.0**：Phase 7 完成（测试补全 + 备份自愈 + 文档），全量 lint 0 error、单测全绿，成熟度在 v2.0 98+ 基础上「质量测试」维度进一步收敛。

## 后续（非 v2.1 阻塞，留待 v2.2+）
- 仪器化测试在真机/CI 运行并接入覆盖率度量（当前仅编译验证）。
- 设计文档 `- [ ]` 漂移项系统性回填。
- 离线 ML ASR 仍为系统框架 best-effort（引入推理框架将破零依赖红线）。
