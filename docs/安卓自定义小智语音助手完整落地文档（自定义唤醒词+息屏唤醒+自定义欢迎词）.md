# 安卓自定义小智语音助手完整落地文档（自定义唤醒词\+息屏唤醒\+自定义欢迎词）

## 文档概述

本文档为**安卓原生APP对接免费小智语音聊天系统**全套定制化解决方案，解决核心需求：替换默认唤醒词、支持锁屏/息屏后台语音唤醒、全局自定义欢迎词与AI人设、本地ASR\+NLU端云混合架构优化，同时适配安卓系统后台保活机制，规避手机耗电、发热、被杀后台等问题，所有方案均为个人非商用免费落地，无接口费用。

## 一、整体技术架构（最终定稿方案）

### 1\.1 架构选型（推荐商用/个人APP）

**本地前置处理 \+ 小智云端AI对话**混合架构，兼顾离线可用、低延迟、低功耗、高对话质量

核心链路：

1. 手机本地：离线语音唤醒 \+ 本地VAD降噪 \+ 本地ASR\+NLU（简单指令本地执行）

2. 云端小智：仅复杂闲聊/多轮问答走云端，不上传原始音频，仅传文本

3. 全程优势：断网可用基础功能、锁屏可唤醒、延迟极低、省电省流量、隐私安全

### 1\.2 架构对比（淘汰纯云端/纯本地方案）

|方案|优点|致命缺点|
|---|---|---|
|纯云端小智|对话能力强、包体小、无发热|无离线唤醒、锁屏无法使用、必须联网|
|纯本地ASR\+NLU|全离线、隐私拉满|闲聊智障、手机发热耗电严重、老机型闪退|
|端云混合（最终方案）|锁屏唤醒、离线指令可用、对话流畅、功耗可控|需集成本地唤醒引擎，开发量稍高|

## 二、核心功能1：自定义离线唤醒词（全程本地、不上云）

### 2\.1 核心原理

小智云端**不提供唤醒检测**，所有唤醒功能依赖安卓本地离线KWS唤醒引擎，支持任意自定义中文唤醒词，完全脱离服务器控制。

推荐唤醒引擎：**Picovoice Porcupine**（免费、中文适配好、占用低、支持自定义训练模型）

### 2\.2 唤醒词定制步骤

1. **自定义唤醒词选型**：建议3\-4个汉字（例：小星小星、你好助手），避免单字、长句，防止误唤醒、识别率低

2. **在线训练专属模型**：Porcupine官网免费训练自定义唤醒词，生成后缀为 `.ppn` 的轻量化模型文件

3. **项目集成**：将模型放入安卓 `assets` 资源目录，代码动态加载

4. **灵敏度调节**：取值0\.3\-0\.9，数值越低越灵敏，越高防误触，推荐默认0\.65

### 2\.3 唤醒核心逻辑

APP后台持续采集16k单声道PCM音频，本地引擎实时检测唤醒词，检测成功后触发：本地欢迎词播报、连接小智WebSocket、开启对话模式，全程音频不上传云端。

## 三、核心功能2：锁屏/息屏后台持续唤醒（安卓保活方案）

### 3\.1 安卓系统底层限制

安卓8\.0以上系统默认冻结后台应用，普通服务锁屏后会被切断麦克风、CPU休眠，**必须通过前台麦克风服务\+权限白名单**实现常驻唤醒。

### 3\.2 必备权限配置（AndroidManifest\.xml）

```Plain Text
<!-- 核心录音权限 -->
<uses-permission android:name="android.permission.RECORD_AUDIO"/>
<!-- 前台语音服务权限 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
<!-- 锁屏CPU保活 -->
<uses-permission android:name="android.permission.WAKE_LOCK"/>
<!-- 网络连接小智网关 -->
<uses-permission android:name="android.permission.INTERNET"/>

<!-- 语音唤醒前台服务声明 -->
<service
    android:name=".VoiceWakeService"
    android:foregroundServiceType="microphone"
    android:exported="false"/>
```

### 3\.3 前台保活服务核心能力

1. 创建系统常驻通知，满足安卓前台服务强制规范，防止被杀后台

2. 获取Partial唤醒锁，锁屏状态下CPU不休眠，持续运行唤醒检测

3. 无人声时VAD休眠推理，降低耗电发热，有人声启动检测

### 3\.4 用户端必备设置（必须开启，否则锁屏失效）

1. 麦克风权限：设置为**始终允许**（禁止仅使用期间允许）

2. 电池管理：关闭APP省电限制、允许后台高耗电

3. 权限管理：开启自启动、后台弹出界面

4. 系统后台清理：将APP加入白名单

## 四、核心功能3：全局自定义欢迎词\+AI人设修改

彻底替换小智官方默认两套话术：**唤醒欢迎语音**、**AI聊天人设与开场白**，全程屏蔽官方默认内容。

### 4\.1 屏蔽小智官方默认欢迎语音（关键代码）

WebSocket监听拦截服务器 `welcome` 类型消息，直接丢弃，不播放官方语音：

```Plain Text
override fun onMessage(ws: WebSocket, text: String) {
    val json = JSONObject(text)
    val msgType = json.getString("type")
    // 拦截屏蔽官方默认欢迎词语音
    if (msgType == "welcome") return
    // 正常处理ASR、LLM、TTS业务消息
}
```

### 4\.2 自定义本地唤醒欢迎词

唤醒词检测成功后，调用**本地离线TTS**播报自定义话术，无网络也可生效，示例：

我在，请问有什么可以帮您？/ 你好，我是专属AI助手，随时听候吩咐

### 4\.3 自定义AI全局人设（握手报文配置）

在WebSocket首次hello握手时，携带自定义prompt，全局修改AI对话风格、开场白、回答逻辑，永久生效：

```Plain Text
{
  "type": "hello",
  "audio_params": {
    "format": "opus",
    "sample_rate": 16000,
    "channels": 1,
    "frame_duration": 60
  },
  "prompt": "你是用户的专属私人AI助手，说话简短温柔、口语化，回答不超过两句话，用户唤醒后主动友好回应，贴合日常对话场景。"
}
```

## 五、本地ASR\+NLU分流逻辑（核心优化）

### 5\.1 分流规则（兼顾离线\+智能）

1. **本地处理（无网可用）**：设备基础指令（调节音量、打开页面、设置闹钟、本地查询），本地ASR转文字\+本地NLU识别意图，不调用云端接口

2. **云端处理（小智免费接口）**：闲聊、问答、多轮对话、知识查询，仅上传识别后的文本，不上传音频，降低功耗与流量

### 5\.2 手机硬件影响优化方案

1. 模型4bit量化压缩，常驻内存控制在350MB以内，避免中低端手机OOM闪退

2. 闲置状态自动释放NLU模型内存，仅保留轻量唤醒检测，降低耗电

3. NPU硬件加速推理，减少CPU负载，解决持续对话发热降频问题

## 六、小智云端接口规范（固定免费配置）

### 6\.1 核心网关信息

- WebSocket网关地址：`wss://api.xiaozhi.me/ws`

- 音频标准：16000Hz、单声道、Opus 60ms/帧（强制标准）

- 鉴权方式：设备6位验证码绑定，个人非商用永久免费

### 6\.2 完整对话时序流程

1. APP启动 → 开启前台唤醒服务 → 持续本地拾音检测唤醒词

2. 检测到自定义唤醒词 → 本地TTS播报自定义欢迎词

3. 建立WebSocket连接，携带自定义prompt完成握手

4. 开启收音，本地音频预处理后上传云端识别

5. 分流处理：简单指令本地执行，闲聊走云端LLM\+TTS

6. 静音超时自动停止收音，回到息屏唤醒待命状态

## 七、避坑指南（安卓开发高频问题）

1. **唤醒词必须本地实现**：云端无唤醒能力，仅靠本地引擎才能实现锁屏唤醒

2. **无法隐藏通知栏**：安卓前台麦克风服务强制展示通知，为系统规范，无规避方案

3. **国产机型适配**：小米、华为、OPPO、vivo后台管控严格，必须手动开启全部权限白名单

4. **音频格式不可错**：必须16k单声道Opus编码，否则云端识别失效

5. **商用限制**：该免费接口仅支持个人非商用，盈利项目需官方授权

## 八、最终落地清单

1. 自定义专属离线唤醒词，支持锁屏/息屏全天候唤醒

2. 完全替换官方欢迎语音，自定义本地播报话术

3. 自定义AI人设与对话风格，全局生效

4. 本地ASR\+NLU端云混合架构，离线基础功能可用

5. 安卓前台保活服务，解决后台被杀、锁屏失效问题

6. 功耗、内存、发热优化，适配高低配安卓手机

> （注：部分内容可能由 AI 生成）
