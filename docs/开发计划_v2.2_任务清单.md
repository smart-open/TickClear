# 点清 TickClear · 开发计划 v2.2 任务清单

> 版本：v2.2.0（2026-07-24）
> 目标：收口 v2.1 计划中「后续（留待 v2.2+）」的三项遗留任务 —— 覆盖率度量与 CI 真机化、设计文档漂移项回填、离线 ML ASR 红线裁决。
> 规则：完成一项即把 `- [ ]` 改为 `✅`，并填写验收结果。v2.2.0 已封板（见 `../release-notes.md`）。

---

## 0. 背景：v2.2 从哪里来

v2.1 计划的「后续」段列了三项遗留任务，明确标注「非 v2.1 阻塞，留待 v2.2+」。本版即为其落地版，外加封板动作（版本升至 2.2.0、打 annotated tag `v2.2.0`）。

> **工程纪律（沿用红线）**：中文全抽离 `strings.xml`；Room 显式 `Migration` 禁 `fallbackToDestructiveMigration`；**不引入新依赖**（Jacoco 随 AGP 内置、CI 用 GitHub Actions 官方/社区 Action，零新增库依赖）；按功能拆 `git commit`、提交前缀 `[fix]/[feature]/[test]/[docs]/[config]/[ci]`。

---

## Phase 8 · 后续遗留任务收口（v2.2）

✅ **V2.25 仪器化测试真机化 + 覆盖率度量**：`app/build.gradle.kts` 启用 `enableUnitTestCoverage` / `enableAndroidTestCoverage`（Jacoco 随 AGP 内置，**零新依赖**）；新增 `.github/workflows/ci.yml` 编排 `lintRelease` + `createDebugUnitTestCoverageReport`（覆盖率产物上传）+ `connectedDebugAndroidTest`（reactivecircus 模拟器跑 `androidTest` 脚手架）。本地已跑通单测覆盖率报告（基线约 4.9% 指令级，覆盖纯逻辑模块；UI/仓库/调度需 Robolectric 或仪器化测试，属后续）。
- 验收：`createDebugUnitTestCoverageReport` 生成 HTML/XML 报告；`./gradlew :app:lintRelease` 0 error。

✅ **V2.26 设计文档 `- [ ]` 漂移项系统性回填**：逐条比对代码后回填 `docs/点清APP_产品设计文档.md` —— 任务组（7 项）、通知栏直接完成、吃药类三操作、位置提醒、静音时段、一键清空确认、振动跟随系统、时间冲突红条（3 项）、空状态/问候语/已完成下沉、语音四类指令/单任务暂停/今日时间线/暂停不触发/软删回收站 30 天/麦克风门控 等均已实现并勾选；**并诚实保留**未实现项（5/30 分钟稍后选项、音效开关、折叠已完成按钮、组级暂停级联、离线热词、方言）与验收指标项（送达率/准确率/耗时/误删率），附代码证据与缺口说明，避免误标。

- [ ] **V2.27 离线 ML ASR（显式保留）**：引入端侧推理框架（如 Vosk / whisper.cpp）将破「零新依赖」红线，且属 best-effort 体验增强而非正确性问题。**v2.2.0 维持系统框架 `android.speech.SpeechRecognizer` 的 best-effort 方案，不引入依赖**。若未来放宽红线，再评估端侧模型。该缺口由 v2.3 的「多档稍后提醒/音效开关/折叠已完成/组级暂停级联」从产品侧补足用户可感知能力。

---

## 验收与里程碑

- **v2.2.0**：Phase 8 完成（V2.25 + V2.26），全量 lint 0 error；覆盖率基线建立；release-notes 追加 v2.2.0 段；版本号升至 2.2.0 并打 annotated tag `v2.2.0`。
- V2.27 显式保留，不计入完成率。

## 红线守住

- 零新库依赖（Jacoco 内置、CI Action 不计为项目依赖）。
- 未改 Room 实体/表（无 Migration）。
- 按功能拆 commit：`[config]` 覆盖率+版本、`[ci]` 工作流、`[docs]` 设计文档回填+release-notes。

## 后续（v2.3+ 候选，非本版阻塞）

- 仪器化测试（Espresso / Compose UI test）覆盖折叠/组级暂停等交互，接 CI 模拟器真机化。
- 设计文档 `- [ ]` 漂移项中「未实现项」若排期实现，对应勾选并补验收证据（这部分在 v2.3 已实现多档稍后/音效/折叠/组级暂停，应回写为已完成）。
- 离线 ML ASR 若未来解除红线，再做端侧推理接入。
