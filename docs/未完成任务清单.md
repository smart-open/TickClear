# 未完成任务清单（点清 TickClear）

> 本文档由 2026-07-26 对三份材料交叉审查产出：
> 1. 《实施计划》`~/.workbuddy/plans/stellar-thunder-newton.md`（Phase 0–7）
> 2. 工程任务清单 `docs/开发计划_v2.5_任务清单.md`
> 3. 任务进程：`git log`（已封板至 `tag v2.5.2`）+ `docs/点清APP_产品设计文档.md` §12 路线图 / §13 开放问题
>
> **总体结论**：工程已从零实现并封板至 **v2.5.2**（Phase 0–7 全部交付、四维成熟度 98.5/100），以下为仍"未完成"的任务，按性质分四类。

---

## 一、纯真机 / 基准 / 人工 QA 缺口（5 项）

> 代码已就绪或无需代码改动，仅待**物理设备 / 特定基建**，CI 无法完成。

| 编号 | 任务 | 阻塞原因 | 状态 |
|---|---|---|---|
| **V2.44** | 提醒送达率 ≥ 98%（含后台被杀） | 需真机 / Doze / 国产 ROM 杀进程场景 | 🔶 |
| **V2.45** | 性能基准：动画 60fps、冷启动 < 500ms | 需 Macrobenchmark / 模拟器基线 | 🔶 |
| **V2.46** | 语音指标评测集（意图 ≥92% / 槽位 ≥88% / 耗时 <4s / 误删 <5%） | 需真机语音评测集 | 🔶 |
| **V2.49** | 语音流水线 QA | 需真机麦克风链路 | 🔶 |
| **V2.51** | 兼容与降级 QA | 需 OEM / 多机型 | 🔶 |

---

## 二、代码层已修复、仅真机走查结转（3 项，非阻塞）

> 代码层修复已在 **v2.5.2 封板后**提交，仅剩**真机人工走查**未闭环，不影响发布。

| 编号 | 代码层状态（已提交） | 仍缺真机走查 |
|---|---|---|
| **V2.47** 核心流程冒烟 | ✅ 数据层仪器化 `CoreFlowSmokeInstrumentedTest` 已在 CI 模拟器实跑（建任务→生成当日实例→完成→读回一致 + 完成幂等 B3 + 回收站软删/恢复） | 真机通知链路 / 助手 Mock / Real 走查 |
| **V2.48** 自适应布局 | ✅ `MedalWall` 改 `GridCells.Adaptive(88.dp)` 宽屏自动增列；`RecycleBin`/`About`/`Debug` 三子屏内容限宽 `widthIn(max=720.dp)`；`Assistant` 删除与侧栏重复的第二个设置齿轮（bug 修复） | 折叠屏真机走查 |
| **V2.50** 无障碍 | ✅ `TaskEditSheet.TimeButton`、组重复锚点时间选择器补 `semantics{role=Button; contentDescription}`（串 `a11y_choose_prefix`）；今日完成率环补 `.minimumInteractiveComponentSize()` 触控目标达 48dp | TalkBack 真机走查 |

---

## 三、产品规划中尚未实现的功能缺口（未来需求，非本轮阻塞）

> 来自 `docs/点清APP_产品设计文档.md` §12 路线图 **LATER(v1.2+)** 与《实施计划》§7「可后置」项，属尚未开发的规划需求。

- 设备间本地迁移（导出文件 / 本地直连，无云同步）
- 任务标签 / 分类
- 更多主题皮肤
- 习惯养成模式
- 语音离线热词 / 端侧识别（可选增强，依赖端侧模型）
- 分享任务组模板（本地导出）
- 《实施计划》§7 后置：真实 ASR SDK 联调（真机配置密钥跑通一次识别 + 建单）、Real 模式连真实小智服务端、端侧 NLU 模型下载

> ✅ **纠偏**：路线图 NEXT(v1.1) 中的 **App Widget 小组件**经代码核查已实现（`app/src/main/java/com/tickclear/app/widget/` 下有 `TodayWidgetProvider`/`TodayWidgetService` 及布局），**不算缺口**；语音 AI 任务管理、打卡热力图、成就系统、位置提醒、主题切换、统计页面也均已在 v1.1/v2.x 落地。

---

## 四、产品设计文档 §13 待决策 / 开放问题

> 非代码阻塞，但会影响未来实现方向，需产品决策。

| 编号 | 问题 |
|---|---|
| Q2 | 音效素材自设计还是用开源 |
| Q3 | 位置提醒用高德还是百度 SDK |
| Q4 | 国内上架软著 |
| Q5 | 内测渠道 |
| Q8 | 语音历史是否默认开启 |
| Q9 | 常驻"嘿点清"语音唤醒 |

---

## 五、本轮同步修正的文档不一致

1. `docs/开发计划_v2.5_任务清单.md` 第 175 行「`androidTest` 源码集为空」已过时 —— 现已含 `AppDatabaseMigrationAndTransactionTest` + `CoreFlowSmokeInstrumentedTest` 两个仪器化测试（CI 模拟器通道实跑）。
2. 同文档第六节的「🔶 仪器化测试代码（androidTest）为空」已过时 —— 已闭环（见上）。
3. App Widget 已实现但任务清单未单独追踪 —— 已补 ✅ 记录（V2.62）。
4. V2.47/V2.48/V2.50 已标 ✅（代码层），但"真机走查结转"在文档中分散 —— 已在本清单第二章合并口径。

---

## 六、一句话总结

- **真正的"任务清单未完成"** = V2.44/45/46/49/51（5 项纯真机/基准缺口）+ V2.47/48/50 的真机走查（3 项代码已完成）。
- **产品规划缺口** = LATER(v1.2+) 的 6 项未来功能 + 《实施计划》§7 后置的 3 项联调。
- **其余** Phase 0–7、封板、文档均已交付。

> 关联文档：`docs/开发计划_v2.5_任务清单.md`（任务总表）、`docs/测试与验收清单.md`（验收 B13/B14 + 2.2）、`docs/成熟度评估.md`（已知限制 §6）。
